{
  "name": "critical-path-builder",
  "version": "0.0.1",
  "description": "Build critical path css and push it into a database",
  "engines": {
    "node": "7.4.0"
  },
  "main": "src/cluster.js",
  "scripts": {
    "start": "node src/cluster.js",
    "dev": "REDIS_URL=redis://127.0.0.1:6379/0 node src/cluster.js",
    "test": "mocha --recursive --debug"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wheeyls/critical-path-builder"
  },
  "keywords": [
    "node",
    "criticalcss"
  ],
  "license": "MIT",
  "dependencies": {
    "bull": "^2.1.0",
    "cluster": "^0.7.7",
    "criticalcss": "^2.1.0",
    "extend": "^3.0.0",
    "request": "^2.79.0"
  },
  "devDependencies": {
    "expect.js": "^0.3.1",
    "mocha": "^3.2.0",
    "redis-mock": "^0.16.0",
    "simple-mock": "^0.7.3"
  }
}
